forbid_telemetry: true

containers:
  lambda: &lambda
    build_directory: .
    build_target: release
  test:
    build_directory: /tmp
    build_target: dev

tasks:
  test-unit:
    description: Run the unit tests
    run:
      command: echo "testing"
      container: test
