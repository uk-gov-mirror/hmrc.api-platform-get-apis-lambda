forbid_telemetry: true

containers:
  lambda: &lambda
    build_directory: .
    build_target: release
  test:
    build_directory: ./
    build_target: dev

tasks:
  test-unit:
    description: Run the unit tests
    run:
      container: test
      command: ./run_all_tests.sh
